# version: "2"  # Temporarily commented for v1 compatibility
run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - unused
    - staticcheck
    - gocritic
