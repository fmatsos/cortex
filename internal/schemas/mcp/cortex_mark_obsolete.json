{
  "name": "cortex_mark_obsolete",
  "description": "Soft-delete a memory by marking it as obsolete. Obsolete memories are excluded from search results by default but can still be retrieved. Use this instead of permanent deletion when a memory is outdated but may still have historical value.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "memory_id": {
        "type": "string",
        "description": "ID of the memory to mark as obsolete"
      },
      "reason": {
        "type": "string",
        "description": "Optional reason for marking this memory obsolete"
      }
    },
    "required": ["memory_id"]
  }
}
