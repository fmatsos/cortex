{
  "name": "cortex_choose_working_consolidation",
  "description": "Return a decision prompt to help select which working memories to consolidate.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "working_memories": {
        "type": "array",
        "description": "Working memory candidates to review",
        "items": {
          "type": "object"
        }
      },
      "selection_goal": {
        "type": "string",
        "description": "Optional goal or focus for the consolidation selection"
      }
    },
    "required": ["working_memories"]
  }
}
