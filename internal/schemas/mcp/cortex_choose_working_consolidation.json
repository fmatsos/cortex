{
  "name": "cortex_choose_working_consolidation",
  "description": "Generate a prompt and context payload to help select which working memories to consolidate.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "working_memories": {
        "type": "array",
        "description": "Working memory candidates to review",
        "items": {
          "type": "object"
        }
      },
      "selection_goal": {
        "type": "string",
        "description": "Optional goal or focus for the consolidation selection"
      },
      "prompt": {
        "type": "string",
        "description": "Optional custom prompt to replace the bundled guidance"
      }
    },
    "required": ["working_memories"]
  }
}
