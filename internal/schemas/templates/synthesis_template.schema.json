{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cortex-ai.github.io/schemas/synthesis-template.schema.json",
  "title": "Cortex Synthesis Template Schema",
  "description": "Schema for customizing synthesis export templates in Cortex",
  "type": "object",
  "properties": {
    "frontmatter": {
      "type": "object",
      "description": "YAML frontmatter configuration",
      "properties": {
        "date_format": {
          "type": "string",
          "default": "2006-01-02T15:04:05Z07:00",
          "description": "Go time format for dates"
        }
      }
    },
    "header": {
      "type": "string",
      "description": "Go template for document header. Available variables: .Intent, .Results (array)",
      "default": "# {{.Intent | title}} - Synthesis\n\nThis document synthesizes {{len .Results}} memories related to \"{{.Intent}}\"."
    },
    "summary_section": {
      "type": "object",
      "description": "Summary section configuration",
      "properties": {
        "title": {
          "type": "string",
          "default": "## Summary",
          "description": "Section title"
        },
        "content": {
          "type": "string",
          "default": "Based on the stored memories, the following information was found:",
          "description": "Section content (can be a Go template)"
        }
      }
    },
    "learnings_section": {
      "type": "object",
      "description": "Learnings section configuration",
      "properties": {
        "title": {
          "type": "string",
          "default": "## Key Learnings",
          "description": "Section title"
        },
        "item_template": {
          "type": "string",
          "description": "Template for each memory item. Variables: .Title, .Score, .Preview",
          "default": "### From: {{.Title}} (score: {{printf \"%.2f\" .Score}})\n\n{{.Preview}}"
        },
        "content_preview_length": {
          "type": "integer",
          "default": 500,
          "minimum": 0,
          "description": "Max characters to show from each memory content"
        }
      }
    },
    "footer": {
      "type": "string",
      "default": "---\n\n*Generated by Cortex - This is a read-only synthesis, not importable.*",
      "description": "Footer template"
    }
  }
}
